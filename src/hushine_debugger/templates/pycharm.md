# PyCharm Local Replay

Create a Python run/debug configuration:

- Module name: `hushine_debugger.cli`
- Parameters: `replay`
- Working directory: this workspace

Set breakpoints in `strategy.py` and start the configuration in Debug mode.
